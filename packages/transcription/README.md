# @aisecretary/transcription

Engine abstraction over OpenAI Whisper API and self-hosted faster-whisper. Per-tenant engine routing (compliance posture + cost tier). Whisper / faster-whisper clients are imported here and **only** here — enforced by Biome rule and CI grep.

See `CLAUDE.md` § Provider abstraction discipline.
