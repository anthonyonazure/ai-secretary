{
  "name": "@aisecretary/crm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check src scripts",
    "test": "vitest run --passWithNoTests",
    "build": "tsc",
    "clean": "rm -rf dist .turbo .tsbuildinfo",
    "check:isolation": "tsx scripts/check-isolation.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
