# Base URL for the AI Secretary API. The web client talks to this for
# auth (Story 1.5a), recordings, transcripts, etc.
#
# Local-dev default tracks the apps/api Fastify server.
VITE_API_URL=http://localhost:3001
